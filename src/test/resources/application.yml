

project:
  base:
    folder: /Users/ivonet/dev/camel-demo


flyway:
  url: "jdbc:h2:mem:camel_demo;MODE=MySQL;INIT=RUNSCRIPT FROM 'classpath:flyway_init.sql'"
  user: sa
  password:
  baseline-on-migrate: true
